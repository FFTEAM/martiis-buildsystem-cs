diff --git a/Makefile.in b/Makefile.in
index 3e6c855..accfe0e 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -40,7 +40,7 @@ KEYOBJS=dropbearkey.o gendss.o genrsa.o
 
 CONVERTOBJS=dropbearconvert.o keyimport.o
 
-SCPOBJS=scp.o progressmeter.o atomicio.o scpmisc.o
+SCPOBJS=scp.o progressmeter.o atomicio.o scpmisc.o compat.o
 
 HEADERS=options.h dbutil.h session.h packet.h algo.h ssh.h buffer.h kex.h \
 		dss.h bignum.h signkey.h rsa.h random.h service.h auth.h \
diff --git a/options.h b/options.h
index def8ccb..f01550f 100644
--- a/options.h
+++ b/options.h
@@ -263,7 +263,8 @@ be overridden at runtime with -I. 0 disables idle timeouts */
 #define DEFAULT_IDLE_TIMEOUT 0
 
 /* The default path. This will often get replaced by the shell */
-#define DEFAULT_PATH "/usr/bin:/bin"
+/* scp is in /opt/pkg/bin... */
+#define DEFAULT_PATH "/usr/bin:/bin:/opt/pkg/bin"
 
 /* Some other defines (that mostly should be left alone) are defined
  * in sysoptions.h */
