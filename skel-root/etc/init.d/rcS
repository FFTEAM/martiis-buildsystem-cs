#!/bin/sh
export PATH=/sbin:/bin:/usr/sbin:/usr/bin
export TZ=`cat /etc/TZ`
mount -n -t tmpfs tmp /dev
/sbin/mknodes 2> /dev/null

dmesg -n 5
for i in /etc/init.d/S??* ; do
	echo "$i start..."
	$i start
done
