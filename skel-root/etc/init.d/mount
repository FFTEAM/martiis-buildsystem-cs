#!/bin/sh

case $1 in
start)
	# mount proc
	/bin/mount -t proc proc /proc
	/bin/mount -a

	echo "Enable Swap space (if exist)..."
	/sbin/swapon -a > /dev/null 2>&1
	exit 0
	;;
stop)
	umount -a
	;;
esac
